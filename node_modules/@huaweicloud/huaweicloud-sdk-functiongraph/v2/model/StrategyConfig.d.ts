export declare class StrategyConfig {
    concurrency: number;
    private 'concurrent_num'?;
    constructor(concurrency?: any);
    withConcurrency(concurrency: number): StrategyConfig;
    withConcurrentNum(concurrentNum: number): StrategyConfig;
    set concurrentNum(concurrentNum: number | undefined);
    get concurrentNum(): number | undefined;
}
