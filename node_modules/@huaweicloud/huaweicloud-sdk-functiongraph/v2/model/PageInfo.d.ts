export declare class PageInfo {
    private 'next_marker';
    private 'previous_marker';
    private 'current_count';
    constructor(nextMarker?: any, previousMarker?: any, currentCount?: any);
    withNextMarker(nextMarker: number): PageInfo;
    set nextMarker(nextMarker: number | undefined);
    get nextMarker(): number | undefined;
    withPreviousMarker(previousMarker: number): PageInfo;
    set previousMarker(previousMarker: number | undefined);
    get previousMarker(): number | undefined;
    withCurrentCount(currentCount: number): PageInfo;
    set currentCount(currentCount: number | undefined);
    get currentCount(): number | undefined;
}
