import { FuncMount } from './FuncMount';
import { MountUser } from './MountUser';
export declare class MountConfig {
    private 'mount_user';
    private 'func_mounts';
    constructor(mountUser?: any, funcMounts?: any);
    withMountUser(mountUser: MountUser): MountConfig;
    set mountUser(mountUser: MountUser | undefined);
    get mountUser(): MountUser | undefined;
    withFuncMounts(funcMounts: Array<FuncMount>): MountConfig;
    set funcMounts(funcMounts: Array<FuncMount> | undefined);
    get funcMounts(): Array<FuncMount> | undefined;
}
